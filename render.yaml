services:
  - type: web
    name: ecosphere-ai
    env: python
    plan: free
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: "streamlit run codefortune.py --server.port=$PORT --server.address=0.0.0.0"
